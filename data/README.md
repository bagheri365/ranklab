# Data

RankLab's primary benchmark is **KuaiRand-Pure**, subject to M0 verification of version, source, checksum, logging-regime semantics, and licensing/redistribution constraints.

Do not commit raw or derived benchmark data to Git.

Expected local layout:

```text
data/
├── raw/
├── interim/
└── processed/
```

M0 should record the exact dataset checksum and provenance in the frozen protocol artifact.
